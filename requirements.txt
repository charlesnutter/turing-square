# Runtime
chess>=1.11
pyserial>=3.5
fastapi>=0.115
uvicorn>=0.30
websockets>=12.0        # uvicorn's WebSocket transport -- FastAPI does not pull it in

# Tests only
pytest>=8.0
httpx2>=2.12            # what starlette's TestClient wants; plain httpx is deprecated
