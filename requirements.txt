# Runtime
chess>=1.11
pyserial>=3.5
fastapi>=0.115
uvicorn>=0.30
websockets>=12.0        # uvicorn's WebSocket transport -- FastAPI does not pull it in

# Tests only
pytest>=8.0
httpx>=0.27             # fastapi.testclient will not import without it
